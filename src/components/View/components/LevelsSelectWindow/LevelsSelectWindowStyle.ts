import { StyleSheet } from "react-native";

export const LevelsSelectWindowStyles = StyleSheet.create({
    container: {
        justifyContent:'center',
        alignContent: 'center',
        alignItems: 'center'
    },
    buttonContainer: {
        gap: 5,
        marginBottom: 30,
        marginTop: 20
    }
})