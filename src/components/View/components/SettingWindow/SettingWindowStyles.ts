import { StyleSheet } from "react-native";

export const SettingWindowStyles = StyleSheet.create({
    localizationContainer :{
        justifyContent: 'center',
        alignContent: 'center',
        alignItems: 'center',
        marginTop: 25,
        marginBottom: 20
    }
})