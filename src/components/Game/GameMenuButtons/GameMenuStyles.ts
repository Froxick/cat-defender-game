import { StyleSheet } from "react-native";

export const GameMenuButtonStyles = StyleSheet.create({
    container: {
        paddingHorizontal: 40,
        paddingVertical: 15,
        borderRadius: 10
    },
    text: {
        fontWeight: 'bold',
        color:'white',
        fontSize: 16
    }
})